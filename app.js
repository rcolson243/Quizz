const form = document.querySelector('.form-quizz');
let tableauResults = [];

form.addEventListener('submit', (e) => {
    e.preventDefault();
    for(let i = 1; i < 6; i++){
       // tableauResults.push(‘‘) je me suis arrêter ici
    }
})